# Tasks — Gemini capability registry

- [ ] Add `src/platform/ai/gemini-model-registry.ts` with dated provider metadata and product-oriented capabilities.
- [ ] Add unit tests for registered authoring/free-tier/live/TTS/embedding metadata and unknown model behavior.
- [ ] Integrate a compatibility check into server configuration without adding automatic model routing.
- [ ] Add server-config tests for a registered incompatible model and an explicit unknown future model.
- [ ] Align `.env.example` with the runtime default model.
- [ ] Run focused unit/type checks, then the repository full verification gate before merge.
