# Plan

1. Replace the item-only observation key with a discriminated subject contract.
2. Project beginner dictation and objective chunk recall to `language_item` subjects.
3. Project guided transfer once to its `activity` subject.
4. Update contract and projection regression tests.
5. Run the full exact-head repository CI and merge only if every gate passes.
