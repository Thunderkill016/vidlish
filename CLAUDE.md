# Claude Code project context

@AGENTS.md

Use `AGENTS.md` as the canonical cross-agent contract. Do not regenerate an independent project plan from stale BMAD artifacts when it conflicts with the current product authority.

For review work, be adversarial about PostgreSQL/RLS semantics, idempotency, concurrency, privacy-safe evidence, answer exposure, Next.js server/client boundaries and misleading learning claims. Prefer concrete defects with reproduction/tests over style commentary.