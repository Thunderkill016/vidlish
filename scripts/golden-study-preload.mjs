// Loaded by the canonical `pnpm study:golden` command before the harness starts.
// The five-person Gate 5 protocol is a predeclared validation instrument, so
// product extensions added later must not silently change its observed surface.
process.env.GOLDEN_STUDY_MODE = "true";
